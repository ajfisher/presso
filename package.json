{
  "name": "presso-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b && node scripts/copy-runtime-assets.mjs",
    "test": "vitest run",
    "dev": "npm run build && node packages/server/dist/cli.js dev examples/basic",
    "presso": "node packages/server/dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
